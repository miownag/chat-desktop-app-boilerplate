{
  "name": "server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "build": "bun build src/index.ts --outdir=dist",
    "start": "bun run dist/index.js"
  },
  "dependencies": {
    "hono": "^4.11.1"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}